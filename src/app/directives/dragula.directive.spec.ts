import { DragulaDirective } from './dragula.directive';

describe('DragulaDirective', () => {
  it('should create an instance', () => {
    const directive = new DragulaDirective();
    expect(directive).toBeTruthy();
  });
});
